# Задача №31: Канзас Сити Шафл
Реализуйте функцию Shuffle, которая перемешивает элементы массива в случайном порядке.

## Build

Build:
```
dotnet build
```

Just run:
```
dotnet run --project src\App.csproj
```
